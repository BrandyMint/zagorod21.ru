%section.food
  .container
    .row
      .col-xs-12.col-md-8
        .meat.mb20.mobile-minus{style: "background-image: url('#{@page.preview.file.standard.url}')" }
          %p.size-20px.bold
            Услуги
            %br/
            фотографа
          %div.visible-xs.visible-sm
            %a.btn.btn-success{href: "#new_order"} Заказать фото
        .desc.mb40
          != @page.body
        .mb40
          - if @page.carousel
            = render 'images'
      .col-xs-12.col-md-4
        .right-price.visible-lg.visible-md.text-center
          .bold.size-18px
            Закажите услуги фотографа
            %br/
        = render 'pages/forms/photo'
        = render 'application/sidebar/phone'
        = render 'application/sidebar/socials'
